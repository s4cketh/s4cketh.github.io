---
import type { ClassValue } from 'clsx'
import { cn } from '@/theme/lib/utils'

interface Props {
  className?: ClassValue
  href?: string
}

const { className, href } = Astro.props;
---

<a class={cn('hover:underline underline-offset-2', className)} href={href}>
  <slot />
</a>
